apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "common.name" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "common.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "common.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "common.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.global.image.repository }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: bap-net-port
              containerPort: {{ .Values.service.port }}
              protocol: TCP

          env:
            # Path to the ONIX adapter config file inside the container.
            # We use the baked-in local-beckn-one-bap.yaml, as in the new docker-compose.
            - name: CONFIG_FILE
              value: "/app/config/local-beckn-one-bap.yaml"

            # Vault settings (adjust/remove if you don't actually use Vault)
            - name: VAULT_ADDR
              value: "http://vault:8200"
            - name: VAULT_TOKEN
              value: "root"

            # Redis address, using the in-cluster Redis service
            - name: REDIS_ADDR
              value: "{{ .Values.global.redisCache.host }}:{{ .Values.global.redisCache.port }}"

            # RabbitMQ address and credentials, using in-cluster RabbitMQ
            - name: RABBITMQ_ADDR
              value: "{{ .Values.global.rabbitMQamqp.host }}:{{ .Values.global.rabbitMQamqp.port }}"
            - name: RABBITMQ_USER
              value: "{{ .Values.global.rabbitMQamqp.username }}"
            - name: RABBITMQ_PASS
              value: "{{ if .Values.global.rabbitMQamqp.password }}{{ .Values.global.rabbitMQamqp.password }}{{ else }}{{ include "getSecretValue" (dict "secretName" "rabbitmq" "namespace" "bap-common-services" "key" "rabbitmq-password") }}{{ end }}"

          # Align container entrypoint with docker-compose: ./server --config=/app/config/local-beckn-one-bap.yaml
          command: ["./server"]
          args: ["--config=/app/config/local-beckn-one-bap.yaml"]

          {{- if .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            # ConfigMap-based default.yaml is currently unused by the adapter,
            # which relies on the baked local-beckn-one-bap.yaml config. We keep
            # this mount for backward compatibility but it is not referenced by CONFIG_FILE.
            - name: bap-network-config
              mountPath: "/usr/src/app/config/default.yaml"
              subPath: default.yaml
              readOnly: true
            # Full ONIX adapter config folder
            - name: bap-adapter-config
              mountPath: "/app/config"
              readOnly: true
            # EBS volumes for logs
            - name: bap-network-logs-ebs-volume
              mountPath: /usr/src/app/logs
      volumes:
        - name: bap-network-config
          configMap:
            name: {{ include "common.name" . }}-config
        - name: bap-adapter-config
          configMap:
            name: {{ include "common.name" . }}-adapter-config
        - name: bap-network-logs-ebs-volume
          persistentVolumeClaim:
            claimName: {{ include "common.name" . }}-logs-pvc
